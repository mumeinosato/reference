prisma migrate dev

model User {
  id        Int      @id @default(autoincrement())
  username  String
  email     String   @unique
  absence   Int?
  attendance Int?
}

npx prisma migrate dev --name add_absence_and_attendance_columns

npx prisma migrate dev